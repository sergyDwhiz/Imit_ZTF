# Pooled PostgreSQL connection string (Neon, Supabase, Railway, Render)
DATABASE_URL=postgresql://user:password@host/dbname

# Secret for the one-time /api/migrate call. Invent a long random string.
MIGRATE_TOKEN=change-me

# Only for a local database without SSL
# PGSSL=false
